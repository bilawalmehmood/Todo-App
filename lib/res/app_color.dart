import 'package:flutter/material.dart';

class AppColor {
  static const Color primaryColor = Colors.black;
  static const Color primaryLightColor = Colors.grey;
  static const Color signInTextColor = Colors.white;
  static const Color buttonColor = Colors.lightBlueAccent;
  static const Color signUpTextColor = Color.fromARGB(0, 0, 0, 0);
  static const Color hintColor = Colors.grey;
  static const Color textColor = Colors.white;
  static const Color cursorColor = Color.fromARGB(245, 117, 157, 177);
}
